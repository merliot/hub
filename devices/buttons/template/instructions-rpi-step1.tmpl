<p>Assemble the buttons device using parts list and circuit diagram above. You can use up to 4 buttons. Connect each button between a GPIO pin and ground, with a 10kΩ pull-up resistor to 3.3V.</p>

<p>Give the buttons a name and a GPIO. GPIO pins <span class="gpio">GPIO06</span>, <span class="gpio">GPIO13</span>, <span class="gpio">GPIO19</span>, <span class="gpio">GPIO26</span> are used in the diagram, as an example with 4 buttons.</p> 